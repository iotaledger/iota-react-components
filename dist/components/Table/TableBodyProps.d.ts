import { ReactNode } from "react";
/**
 * Properites for Table Body.
 */
export interface TableBodyProps {
    /**
     * The children elements to display as content.
     */
    children: ReactNode | ReactNode[];
}

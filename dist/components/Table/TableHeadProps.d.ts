import { ReactNode } from "react";
/**
 * Properties for the Table Head.
 */
export interface TableHeadProps {
    /**
     * The children elements to display as content.
     */
    children: ReactNode | ReactNode[];
}

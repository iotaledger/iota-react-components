import { ReactNode } from "react";
/**
 * Properties for Layout App Single.
 */
export interface LayoutAppSingleProps {
    /**
     * The children elements to display as content.
     */
    children?: ReactNode | ReactNode[];
}

import { ReactNode } from "react";

/**
 * The properties for the CheckboxContainer component.
 */
export interface CheckboxContainerProps {
    /**
     * The children elements to display as content.
     */
    children?: ReactNode | ReactNode[];
}
